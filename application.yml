spring:
  application:
    name: microservicedemo

greeting:
  hello: hello world from CLOUD yaml

my:
  list: one,two,three,four

app:
  name: myApp
  description: the app, ${app.name}, is used mainly to say hello

db:
  connection: connection-StringCloud
  host: 127.0.0.1
  port: 1200

management:
  endpoints:
    web:
      exposure:
        include: "*"